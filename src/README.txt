写个注释便于记忆（代码写久了总是忘）。

目录结构
src
  - utils
        - label_utils
          : 输出为dict，key为id，value为label。
        - csv_utils
          : 合并props为一个props；输出props到csv；读取csv。
        - glcm_utils
          : 输出为dict，key为id，value为props。



